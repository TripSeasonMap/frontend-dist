/* uncomment for emulate ios */

/* const cordova = {
	platformId: 'ios',
};

window.appMetrica = {
	activate: () => {},
};

window.onload = () => document.dispatchEvent(new CustomEvent('deviceready')); */
